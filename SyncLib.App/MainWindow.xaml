<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="SyncLib_App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:SyncLib_App"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="SyncLib"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" Title="SyncLib">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="Assets/AppIcon.ico" />
            </TitleBar.IconSource>
        </TitleBar>

        <NavigationView x:Name="NavView" Grid.Row="1" IsBackButtonVisible="Collapsed" ItemInvoked="NavView_ItemInvoked" Loaded="NavView_Loaded">
            <NavigationView.MenuItems>
                <NavigationViewItem Icon="Home" Content="Geral" Tag="DashboardPage" />
            </NavigationView.MenuItems>
            <NavigationView.FooterMenuItems>
                <!-- O item de configuração padrão também pode ser usado mas vamos forçar uma tag caso ele seja customizado -->
            </NavigationView.FooterMenuItems>
            
            <Frame x:Name="RootFrame" />
        </NavigationView>
    </Grid>
</Window>
